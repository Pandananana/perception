## Documentation

### Pydoc

Run `uv run python -m pydoc -b` to start the pydoc server.

Then open `http://localhost:56432` in your browser.

### Python help function

Run `help(cv2.imread)` to get the documentation for the `imread` function.